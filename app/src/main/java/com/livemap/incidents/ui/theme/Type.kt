package com.livemap.incidents.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 typography. Kept as its own file so type tweaks live in one place.
val AppTypography = Typography()
