package com.livemap.incidents.ui.theme

import androidx.compose.ui.graphics.Color

// Brand / surface palette
val Primary = Color(0xFF1565C0)
val PrimaryDark = Color(0xFF90CAF9)
val Surface = Color(0xFFFAFAFA)
val SurfaceDark = Color(0xFF121212)

// Severity palette — reused by markers, list badges and the detail screen so the
// colour language stays consistent across the whole app.
val SeverityLow = Color(0xFF2E7D32)
val SeverityMedium = Color(0xFFF9A825)
val SeverityHigh = Color(0xFFEF6C00)
val SeverityCritical = Color(0xFFC62828)
